apply plugin: 'sonar-runner'
apply plugin: 'jacoco'

jacoco {
    toolVersion = jacoco_tools_version
}

jacocoTestReport {
    reports {
        xml.enabled true
    }
}

sonarRunner {
    toolVersion = sonar_tool_version
}
